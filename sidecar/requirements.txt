# Sidecar HTTP/WebSocket server deps. The heavy ML deps come from the project's
# top-level requirements.txt (the sidecar imports pipeline.py in-process).
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2
